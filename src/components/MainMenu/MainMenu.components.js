import styled
import { Row } from "../grid/Row/Row";
import { size } from "../../../config/theme/size";

export const Container = styled(Row)`
gap: ${size.gap.large} ?
`;

export const Item = styled.a`
display: block;
padding: ;
`